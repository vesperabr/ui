{% include 'header.twig' %}

    <div class="Doc-wrapper">
        <div class="Doc-sidebar">
            <a class="logo _with-margin" href="{{ base_url }}">Monalisa</a>

            <nav>
                {{ nested_pages | only('conceitos') | navigation }}
                {{ nested_pages | only('base') | navigation }}
                {{ nested_pages | only('componentes') | navigation }}
            </nav>
        </div>

        <div class="Doc-page">
            <div class="Doc-title">{{ current_page.title }}</div>
            {{ content }}
        </div>
    </div>

{% include 'footer.twig' %}
